[package]
name = "transform"

[dependencies]
handlebars = "5.1.2"
regex      = "1.10"
serde      = { version = "1", features = ["derive"] }
swc_cached = "0.3.20"
swc_core   = { version = "0.95.0", features = ["ecma_plugin_transform"] }
